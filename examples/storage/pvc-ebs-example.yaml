apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-ebs-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-gp3
  resources:
    requests:
      storage: 20Gi
